DO BEGIN    DECLARE Type VARCHAR(20) := '[%C]';  DECLARE AbsEntry VARCHAR(20) := '[%D]';   IF :Type = 'PickList' THEN    select	(t2."ItemCode"|| '/' || T0."AbsEntry"|| '/' || t2."WhsCode"|| '/' || t2."Quantity" || '/' || 	T1."PickEntry") as "QRData" 	FROM OPKL T0 	INNER JOIN PKL1 T1 ON T0."AbsEntry" = T1."AbsEntry" 	LEFT JOIN RDR1 T2 ON T2."DocEntry" = T1."OrderEntry" AND T1."OrderLine" = T2."LineNum" AND "BaseObject" = 17	LEFT JOIN ORDR T3 ON T2."DocEntry" = T3."DocEntry" and T3."ObjType" = T1."BaseObject" 	LEFT JOIN RDR12 T4 ON T4."DocEntry" = T3."DocEntry"	LEFT JOIN OITM T5 ON T5."ItemCode" = T2."ItemCode"	WHERE T0."AbsEntry" = :AbsEntry; END IF;   END;